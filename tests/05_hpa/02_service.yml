apiVersion: v1
kind: Service
metadata:
  name: consumer-svc
  namespace: hpa
spec:
  selector:
    app: consumer
  type: NodePort
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30001
